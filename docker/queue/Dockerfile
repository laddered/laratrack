# ВНИМАНИЕ: Этот Dockerfile для очередей (queue) сейчас не используется.
# Оставлен на будущее, если потребуется запускать очереди через отдельный контейнер.
# Можно удалить этот файл, если очереди не планируются.
#
FROM php:8.3-fpm-alpine

# Оставляем контейнер в интерактивном режиме, чтобы он не завершался
CMD ["sh", "-c", "while [ ! -f artisan ]; do echo 'Waiting for artisan...'; sleep 60; done; php artisan queue:work"] 